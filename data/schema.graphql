type Topic {
  id: String
  title: String
  description: String
  thumbnail: String
  tags: [Tag]
}

type Tag {
  id: String
  name: String
}

type Query {
  allTopics: [Topic]
}

schema {
  query: Query
}
